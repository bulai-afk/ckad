# HTTP-only (до установки SSL). После сертификатов: install-ssl-on-server.sh
server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;

    include /etc/nginx/snippets/ckad-proxy.inc;
}
